describe('BackendService', function() {

  beforeEach(module('HackSheffield'));

  it('should ...', inject(function(BackendService) {

    //expect(BackendService.doSomething()).toEqual('something');

  }));

});
