describe('logFilter', function() {

    beforeEach(module('HackSheffield'));

    it('should ...', inject(function($filter) {

        var filter = $filter('logFilter');

        expect(filter('input')).toEqual('output');

    }));

});
